package com.SEBN.backend.repository;

import com.SEBN.backend.models.InscrEvent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InscrEventRepository extends JpaRepository<InscrEvent, Long> {
}
